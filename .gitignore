node_modules/
frontend/node_modules/
backend/node_modules/
frontend/dist/
*.log
.env
backend/.agent-sessions.json
backend/memories/
backend/xp-data.json
backend/users.json
backend/devices.json
backend/usage.json
backend/error-log.json
backend/chatOverlayConfig.json
backend/piper/
# All runtime user data — the SQLite DB, uploaded avatars, chat overlay
# backgrounds and overlay assets. Ignore the whole directory rather than
# listing subdirectories: overlayAssets/ was added later and was NOT covered,
# so user uploads were one `git add -A` away from being committed.
backend/data/
