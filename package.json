{
  "name": "aicompanion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "test": "npm --prefix backend test && npm --prefix frontend test",
    "dev": "concurrently --kill-others-on-fail -n backend,frontend \"node backend/index.js\" \"npm --prefix frontend run dev -- --open\"",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
